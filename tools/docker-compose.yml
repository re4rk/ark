version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: ark-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=ark123!
      - MYSQL_DATABASE=ark
      - MYSQL_USER=ark
      - MYSQL_PASSWORD=ark123!
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: --default-authentication-plugin=mysql_native_password
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "ark", "-park123!"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - network

  redis:
    image: redis:7-alpine
    container_name: ark-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - network

  minio:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z
    container_name: ark-minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - network


volumes:
  mysql-data:
  redis-data:
  minio-data:

networks:
  network:


